---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: Quarkus Config Reproducer
description: Quarkus Config reproducer for null items for runtime config in Quarkus Unit Tests
metadata:
  keywords:
    - config
    - config-reproducer
    - smallrye-config-issue
  status: "stable"
  unlisted: false
  config:
    - "quarkus.reproducer."